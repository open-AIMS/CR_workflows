% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_average_drc.R
\name{fit_cr_drc_ma}
\alias{fit_cr_drc_ma}
\title{Fit every candidate mean function and average them by Akaike weight}
\usage{
fit_cr_drc_ma(data, test_type, validate = TRUE, ...)
}
\arguments{
\item{data}{A data frame holding one test.}

\item{test_type}{Test-type identifier, one of \code{cr_test_types()$id}.}

\item{validate}{Whether to run \code{\link[=check_cr_data]{check_cr_data()}} first.}

\item{...}{Passed to \code{\link[=fit_cr_drc]{fit_cr_drc()}}.}
}
\value{
An object of class \code{cr_drc_ma}: a list with elements \code{fits} (the
converged fits, named by mean function), \code{weights}, \code{comparison},
\code{test_type} and \code{data}.
}
\description{
Fits each function returned by \code{\link[=drc_candidates]{drc_candidates()}} and combines them into one
model-averaged fit, so that the reported uncertainty includes the choice of
curve form rather than being conditional on one function having been picked
correctly. This is the \code{drc} counterpart of what \code{bayesnec} does with
stacking weights.
}
\details{
Weights are Akaike weights, \verb{exp(-delta AIC / 2)} normalised to sum to one,
computed from the log-likelihood returned by the internal \code{cr_drc_loglik()}.
That matters for the count test types: \code{drc} reports a log-likelihood of the
wrong sign and magnitude for \code{type = "Poisson"}, so weights taken from its
AIC would be meaningless, and \code{\link[drc:maED]{drc::maED()}} fails on those fits outright.
}
\examples{
\donttest{
ma <- fit_cr_drc_ma(algal_growth, "algal_growth", validate = FALSE)
cr_model_weights(ma)
}
}
