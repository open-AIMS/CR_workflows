% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_checks.R
\name{check_cr_data}
\alias{check_cr_data}
\title{Validate a concentration-response dataset against its test type}
\usage{
check_cr_data(data, test_type, min_conc_levels = 5, min_reps = 3)
}
\arguments{
\item{data}{A data frame holding one test.}

\item{test_type}{Test-type identifier, one of \code{cr_test_types()$id}.}

\item{min_conc_levels}{Minimum number of distinct non-control concentrations
below which an issue is recorded. Five is the usual guideline minimum for
regression-based estimation.}

\item{min_reps}{Minimum replicates per concentration below which an issue is
recorded.}
}
\value{
An object of class \code{cr_check}: a list with elements \code{test_type},
\code{label}, \code{n_rows}, \code{n_conc}, \code{issues} and \code{design}.
}
\description{
Applies the checks a laboratory analyst would otherwise make by eye before
fitting: that the expected columns are present, that concentrations and
responses fall within admissible ranges for the response type, that a control
is present, that replication is adequate, and that the response declines with
concentration over the tested range. Problems that make a fit impossible are
raised as errors. Problems that only threaten reliability are returned in the
result so that the analyst can record and override them, rather than being
silently corrected.
}
\examples{
data(algal_growth)
check_cr_data(algal_growth, "algal_growth")
}
