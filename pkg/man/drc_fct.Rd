% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_drc.R
\name{drc_fct}
\alias{drc_fct}
\title{Construct a drc mean function by name}
\usage{
drc_fct(name)
}
\arguments{
\item{name}{Name of a drc mean function, for example \code{"LL.3"}.}
}
\value{
A drc mean function object.
}
\description{
drc mean functions read their own name out of \code{match.call()}, so they must be
constructed by an ordinary named call rather than with \code{do.call()} on the
function object, which leaves \code{match.call()[[1]]} holding a closure and fails.
This helper exists so that a workflow can turn the name selected by
\code{\link[=compare_drc_models]{compare_drc_models()}} back into the object \code{\link[=fit_cr_drc]{fit_cr_drc()}} needs.
}
\examples{
drc_fct("LL.3")$name
}
