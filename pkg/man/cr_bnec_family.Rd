% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bayesnec.R
\name{cr_bnec_family}
\alias{cr_bnec_family}
\title{Response family for the bayesnec engine}
\usage{
cr_bnec_family(test_type)
}
\arguments{
\item{test_type}{Test-type identifier, one of \code{cr_test_types()$id}.}
}
\value{
A \code{brms} family object.
}
\description{
Builds the \code{brms} family object used by \code{\link[=fit_cr_bayesnec]{fit_cr_bayesnec()}} for a given test
type, with the link set explicitly.
}
\details{
The link is always \code{"identity"}. This is not a stylistic choice: \code{bayesnec}
sets \code{link = "identity"} for every family it accepts, so that \code{top}, \code{bot}
and \code{nec} remain on the natural response scale. Several \code{brms} families
default to something else (\code{Beta()} and \code{binomial()} to logit, \code{Gamma()} to
inverse), and a family constructed with its default link produces a different
model that still samples cleanly and reports healthy convergence diagnostics.
Stating the link here keeps any hand-written \code{brms} comparison honest.
}
