% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates.R
\name{cr_ecx}
\alias{cr_ecx}
\title{Extract ECx estimates from a fit of either engine}
\usage{
cr_ecx(fit, ecx_val = NULL, test_type = NULL, level = 0.95, ...)
}
\arguments{
\item{fit}{A \code{bayesnecfit}, \code{bayesmanecfit} or \code{drc} object.}

\item{ecx_val}{ECx levels as percentages. Defaults to the registry value for
the test type.}

\item{test_type}{Test-type identifier. Only needed for \code{drc} fits not created
by \code{\link[=fit_cr_drc]{fit_cr_drc()}}, which records it as an attribute.}

\item{level}{Interval width. Defaults to 0.95.}

\item{...}{Passed to the underlying engine function.}
}
\value{
A data frame with columns \code{engine}, \code{estimate_type}, \code{level},
\code{estimate}, \code{lower}, \code{upper} and \code{interval}.
}
\description{
Returns ECx estimates in one common format regardless of which engine
produced the fit, so that a results table has the same columns whether the
laboratory runs \code{bayesnec} or \code{drc}.
}
\details{
The interval reported is not the same quantity in the two engines and the
\code{interval} column records which was used. For \code{bayesnec} it is a credible
interval from the posterior of the model-averaged prediction. For \code{drc} it is
a delta-method confidence interval. They are not interchangeable and should
not be presented in a single column without that label.
}
