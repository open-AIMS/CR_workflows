% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{cr_render_workflow}
\alias{cr_render_workflow}
\title{Render a workflow document and file its report}
\usage{
cr_render_workflow(
  engine = c("drc", "bayesnec"),
  test_type,
  sample_id = "example",
  data_file = NULL,
  root = cr_project_root(),
  backend = "cmdstanr",
  quiet = TRUE,
  ...
)
}
\arguments{
\item{engine}{Either \code{"drc"} or \code{"bayesnec"}.}

\item{test_type}{Test-type identifier, one of \code{cr_test_types()$id}.}

\item{sample_id}{Identifier for the sample, used in the output file names.}

\item{data_file}{Path to a csv of the sample data, or \code{NULL} to use the
shipped example dataset for the test type.}

\item{root}{Project root. Defaults to \code{\link[=cr_project_root]{cr_project_root()}}.}

\item{backend}{For the bayesnec engine, the Stan interface. \code{"cmdstanr"} is
the default because it is the faster of the two. Both compile Stan models
and so both need a C++ toolchain.}

\item{quiet}{Whether to suppress Quarto's progress output.}

\item{...}{Further parameters passed to the document, for example
\code{average = FALSE} or \code{save_fit = FALSE}.}
}
\value{
Invisibly, the path to the filed report.
}
\description{
Renders the workflow for one sample and moves the resulting html into
\verb{outputs/reports/}, so that the report sits alongside the figure, table and
fit that the same run produced.
}
\details{
The document is rendered in a scratch copy rather than in place. Quarto
writes its html beside the source and uses a fixed intermediate name, so two
renders of the same test type at once would overwrite each other's working
files. Copying first makes concurrent renders safe, which the app relies on,
and leaves the \verb{workflows/} tree untouched by a run.
}
