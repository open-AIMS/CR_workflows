% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates.R
\name{cr_nec}
\alias{cr_nec}
\title{Extract the threshold estimate from a bayesnec fit}
\usage{
cr_nec(fit, type = c("auto", "nec", "nsec"), level = 0.95, ...)
}
\arguments{
\item{fit}{A \code{bayesnecfit}, \code{bayesmanecfit} or \code{drc} object.}

\item{type}{One of \code{"auto"}, \code{"nec"} or \code{"nsec"}. \code{"auto"} chooses the label
from the models in the fit, as described above. The explicit values force
the corresponding \code{bayesnec} function and label.}

\item{level}{Interval width. Defaults to 0.95.}

\item{...}{Passed to the underlying engine function. The \code{drc} methods also
accept \code{reference}, which selects what the ECx is referenced to: \code{"control"}
(the default) references it to the fitted control response, which is the
convention in ecotoxicological reporting and what \code{bayesnec::ecx()} returns;
\code{"range"} references it to the fitted response range, which is drc's own
convention. The two coincide wherever the fitted lower limit is zero.}
}
\value{
A data frame in the same format as \code{\link[=cr_ecx]{cr_ecx()}}, with \code{estimate_type}
one of \code{"NEC"}, \code{"NSEC"} or \code{"N(S)EC"}.
}
\description{
Returns the no-effect concentration and labels it with the quantity that was
actually estimated. Three labels are possible and they are not
interchangeable:
}
\details{
\describe{
\item{\code{NEC}}{Every model in the fit carries the \code{nec} threshold parameter,
so the estimate is that parameter.}
\item{\code{NSEC}}{No model carries a threshold, so the estimate is derived from
the smooth fitted curve.}
\item{\verb{N(S)EC}}{The candidate set mixes threshold and smooth models, so the
model-averaged estimate is a weighted mixture of \code{nec} and NSEC draws. It
is not a NEC, and reporting it as one overstates what was estimated.}
}

The distinction was found by \code{bayesnec} itself: fitting the \code{decline} model
group produces a mixed candidate set, and \code{bayesnec::nec()} warns that its
return value is a mixture. Labelling that result \code{NEC} in a report would be
wrong.
}
