% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_stan.R
\name{check_stan_toolchain}
\alias{check_stan_toolchain}
\title{Check the Stan toolchain before fitting}
\usage{
check_stan_toolchain(
  backend = c("cmdstanr", "rstan"),
  stages = c("packages", "toolchain", "cmdstan", "stan", "brms"),
  quiet = TRUE
)
}
\arguments{
\item{backend}{Which Stan interface to check, \code{"cmdstanr"} or \code{"rstan"}.}

\item{stages}{Which stages to run. Later stages are skipped automatically
once an earlier one has failed, because their result would not be
interpretable.}

\item{quiet}{Whether to suppress compiler and sampler output.}
}
\value{
An object of class \code{cr_stan_check}, a data frame with one row per
stage and columns \code{stage}, \code{status} (\code{"pass"}, \code{"fail"} or \code{"skip"}),
\code{detail} and \code{seconds}.
}
\description{
Runs the installation checks that the \code{cmdstanr} and \code{rstan} documentation
recommend, in the order the layers depend on each other, and reports which
layer fails. This is a better first test than fitting a \code{bayesnec} model: it
is faster, and it says which part of the installation is at fault instead of
failing somewhere inside Stan.
}
\details{
The last two stages compile a Stan model, which takes some tens of seconds
the first time. That compilation is the point of the check: a toolchain can
look correctly configured and still fail to build a model.
}
\examples{
\donttest{
check_stan_toolchain()
check_stan_toolchain("rstan", stages = c("packages", "toolchain"))
}
}
