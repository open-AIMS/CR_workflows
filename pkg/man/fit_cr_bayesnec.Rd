% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bayesnec.R
\name{fit_cr_bayesnec}
\alias{fit_cr_bayesnec}
\title{Fit a concentration-response model with the bayesnec engine}
\usage{
fit_cr_bayesnec(data, test_type, model = NULL, validate = TRUE, ...)
}
\arguments{
\item{data}{A data frame holding one test.}

\item{test_type}{Test-type identifier, one of \code{cr_test_types()$id}.}

\item{model}{\code{bayesnec} model or model-group name. Defaults to the registry
value for the test type; \code{"decline"} for monotonic endpoints and \code{"all"}
where a hormetic response is plausible.}

\item{validate}{Whether to run \code{\link[=check_cr_data]{check_cr_data()}} first and stop on a
structural error. Set to \code{FALSE} only when the data have already been
checked in the same session.}

\item{...}{Further arguments passed to \code{\link[bayesnec:bnec]{bayesnec::bnec()}}, for example
\code{iter}, \code{chains}, \code{seed} or \code{control}.}
}
\value{
A \code{bayesnecfit} or \code{bayesmanecfit} object.
}
\description{
Wraps \code{\link[bayesnec:bnec]{bayesnec::bnec()}} with the family, link and candidate model set
recorded for the test type, so that every laboratory fitting the same test
type starts from the same specification.
}
