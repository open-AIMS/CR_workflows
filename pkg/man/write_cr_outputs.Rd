% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{write_cr_outputs}
\alias{write_cr_outputs}
\title{Write the standard output set for one analysis}
\usage{
write_cr_outputs(
  fit,
  plot,
  results,
  stem,
  root = getOption("crworkflows.output_root", "outputs"),
  width = 6,
  height = 4.5,
  save_fit = TRUE
)
}
\arguments{
\item{fit}{A fitted model object.}

\item{plot}{A \code{ggplot} object.}

\item{results}{A results table, as returned by \code{\link[=cr_results_table]{cr_results_table()}}.}

\item{stem}{File stem, typically \code{"<sample_id>_<test_type>_<engine>"}.}

\item{root}{Output root directory.}

\item{width, height}{Figure dimensions in inches.}

\item{save_fit}{Whether to save the fitted model object. Model-averaged
Bayesian fits are very large: the \code{algal_growth} example, twelve models at
four chains of 4000 iterations, serialises to 423 MB. A laboratory
archiving many tests should set this to \code{FALSE} and re-fit from the
recorded seed and package versions when a fit is needed again. \code{drc} fits
are a few hundred kilobytes and can be kept without difficulty.}
}
\value{
Invisibly, a named character vector of the paths written.
}
\description{
Saves the figure, the results table and the fitted model object to the
project \verb{outputs/} tree under a common stem, so that every analysis leaves
the same set of files behind and a report can be traced back to the fit that
produced it.
}
